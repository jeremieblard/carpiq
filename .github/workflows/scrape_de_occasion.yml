name: CarPIQ — Scrape Allemagne Occasion

on:
  schedule:
    - cron: '0 21 1-7 * 3'
  workflow_dispatch:

jobs:

  scrape-vag:
    name: VAG Group
    runs-on: ubuntu-latest
    timeout-minutes: 340
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Chrome
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -q
          sudo apt-get install -y google-chrome-stable
      - run: pip install selenium webdriver-manager
      - run: git pull origin main --rebase || true
      - name: Scrape VAG
        run: |
          python3 carpiq_price_scraper_v6.py --country de --brand vw --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand audi --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand skoda --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand seat --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand cupra --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand porsche --output prices_de_v6.json
      - name: Commit
        run: |
          git config user.name "CarPIQ Bot"
          git config user.email "bot@carpiq.eu"
          git add prices_de_v6.json
          git diff --staged --quiet || git commit -m "🤖 Scrape Allemagne occasion — VAG [$(date +%Y-%m-%d)]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  scrape-premium:
    name: Premium DE
    runs-on: ubuntu-latest
    timeout-minutes: 340
    needs: scrape-vag
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Chrome
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -q
          sudo apt-get install -y google-chrome-stable
      - run: pip install selenium webdriver-manager
      - run: git pull origin main --rebase || true
      - name: Scrape Premium
        run: |
          python3 carpiq_price_scraper_v6.py --country de --brand bmw --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand mercedes --output prices_de_v6.json
      - name: Commit
        run: |
          git config user.name "CarPIQ Bot"
          git config user.email "bot@carpiq.eu"
          git add prices_de_v6.json
          git diff --staged --quiet || git commit -m "🤖 Scrape Allemagne occasion — Premium [$(date +%Y-%m-%d)]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  scrape-fr-jp:
    name: FR + JP
    runs-on: ubuntu-latest
    timeout-minutes: 340
    needs: scrape-premium
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Chrome
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -q
          sudo apt-get install -y google-chrome-stable
      - run: pip install selenium webdriver-manager
      - run: git pull origin main --rebase || true
      - name: Scrape FR + JP
        run: |
          python3 carpiq_price_scraper_v6.py --country de --brand peugeot --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand renault --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand citroen --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand dacia --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand toyota --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand lexus --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand honda --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand mazda --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand suzuki --output prices_de_v6.json
      - name: Commit
        run: |
          git config user.name "CarPIQ Bot"
          git config user.email "bot@carpiq.eu"
          git add prices_de_v6.json
          git diff --staged --quiet || git commit -m "🤖 Scrape Allemagne occasion — FR+JP [$(date +%Y-%m-%d)]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  scrape-rest:
    name: Coréens + Divers
    runs-on: ubuntu-latest
    timeout-minutes: 340
    needs: scrape-fr-jp
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install Chrome
        run: |
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo apt-key add -
          echo "deb [arch=amd64] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update -q
          sudo apt-get install -y google-chrome-stable
      - run: pip install selenium webdriver-manager
      - run: git pull origin main --rebase || true
      - name: Scrape Reste
        run: |
          python3 carpiq_price_scraper_v6.py --country de --brand hyundai --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand kia --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand genesis --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand ford --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand opel --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand volvo --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand mini --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand tesla --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand nissan --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand mg --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand byd --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand polestar --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand landrover --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand alfa --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand fiat --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand ds --output prices_de_v6.json
          python3 carpiq_price_scraper_v6.py --country de --brand jeep --output prices_de_v6.json
      - name: Commit
        run: |
          git config user.name "CarPIQ Bot"
          git config user.email "bot@carpiq.eu"
          git add prices_de_v6.json
          git diff --staged --quiet || git commit -m "🤖 Scrape Allemagne occasion — Reste [$(date +%Y-%m-%d)]"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
